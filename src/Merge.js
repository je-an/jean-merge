define([], function () {
    /**
     * Merges two objects into one 
     * @alias Merge 
     * @constructor
     * @param {Object} options - options object
     */
    var Merge = function(options){

    };

    return Merge;
});